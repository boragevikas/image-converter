<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Image Converter</title>
    <link rel="stylesheet" href="/styles.css">
    <!-- SEO Meta Tags -->
    <meta name="description" content="Free online image converter between JPG, PNG, WebP, GIF, BMP, and TIFF formats">
    <meta name="keywords" content="image converter, jpg to png, webp converter, online image tools">
</head>
<body>
    <header>Image Format Converter</header>
    <div class="container">
        <h2>Convert Your Images Easily</h2>
        <form action="/convert" method="POST" enctype="multipart/form-data">
            <div class="form-group">
                <input type="file" name="image" accept="image/*" required>
            </div>
            <div class="form-group">
                <select name="format" required>
                    <% formats.forEach(format => { %>
                        <option value="<%= format %>"><%= format.toUpperCase() %></option>
                    <% }); %>
                </select>
            </div>
            <button type="submit">Convert</button>
        </form>
    </div>
    <footer>&copy; 2025 Image Converter. All Rights Reserved.</footer>
</body>
</html>
